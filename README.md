## Intructions
To run in the browser, just open index.html

To build a debug apk with Capacitor, run:
- pnpm add android
- pnpm sync
- pnpm build-debug

The apk will be in ../android/app/build/outputs/apk/app-debug.apk